#!/bin/bash -e

cd /app/api

echo "Installing gems..."
bundle install

mkdir -p storage tmp/pids

echo "Preparing database..."
bin/rails db:prepare

rm -f tmp/pids/server.pid

echo "Starting application..."
exec "$@"
