# PostgreSQL (used by api when running outside Docker)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/api_development

# Frontend GraphQL endpoint
VITE_GRAPHQL_URL=http://localhost:3000/graphql
